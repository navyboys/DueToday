= render 'shared/content_title', title: 'Sign Up'
.well
  = bootstrap_form_for(@user) do |f|
    = f.email_field :email
    = f.password_field :password
    = f.text_field :name
    = f.submit 'Sign Up'
