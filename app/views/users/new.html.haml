%h2.title Sign Up
#sign-in-form
  = bootstrap_form_for(@user) do |f|
    = f.email_field :email
    = f.password_field :password
    = f.text_field :name
    = f.submit 'Sign Up'
    = link_to 'Forgot password?', forgot_password_path
